defaults:
  - _self_

dataset:
  root: "LibriSpeech"
  train_split: "data/LibriSpeech/train-clean-100"
  test_split: "data/LibriSpeech/test-clean"
  batch_size: 16 
  num_workers: 1
  noise_dir: "noise_fullband"
  max_audio_length: 160000  # 10s at 16kHz
  augmentations:
    time_stretch: true
    pitch_shift: true
    reverberation: true
    noise: true

model:
  name: "hubert-base-ls960"
  layer: 9
  vocab_size: 500
  kind_kmeans: "kmeans"
  quantizer:
    hidden_dim: 256

training:
  epochs: 25
  learning_rate: 1e-4
  log_interval: 100
  checkpoint_dir: "checkpoints/quantizer"
  resume_from: null
  n_iterative_pseudolabeling: 3 # set > 0 for iterative training
  lr_scheduler:
    _target_: "torch.optim.lr_scheduler.CosineAnnealingLR"
    T_max: 25
    eta_min: 1e-6
  lr_scheduler_start_epoch: 10
